return {
    -- Values are clamped by main.lua so a broken config cannot stall a match.
    Enabled = true,
    DrawAtBattleStart = true,
    DrawIntervalSeconds = 60,
    MaxOwnedCards = 3,
    CardsPerOffer = 3,
    SelectionSeconds = 60,
}

